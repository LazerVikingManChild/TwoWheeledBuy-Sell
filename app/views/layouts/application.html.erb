<!DOCTYPE html>

<html lang="en">
    <head>
        <title>TwoWheeledBS</title>
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        
        <!-- Bootstrap Link -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">

        <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    </head>

    <body class="background">

        <div class="navbar navbar-text">

            <div class="container-fluid" style="justify-content: flex-end">
                <% if user_signed_in? %>
                    <%= image_tag "moto-man-mint.png", class: "moto-man-lite" %>
                    <ul class="nav-ul">
                        <li class="nav-item">
                            <a><%= link_to "Posts", "/posts", class: "navbar-link" %></a>
                        </li>
                        <li class="nav-item">
                            <a><%= link_to "New Post", "/posts/new", class: "navbar-link" %></a>
                        </li>
                        <li class="nav-item">
                            <a><%= link_to 'Inbox', "/chats", class: "navbar-link" %></a>
                        </li>
                        <li class="nav-item">
                            <a><%= link_to 'Edit Profile', edit_user_registration_path, class: "navbar-link" %></a>
                        </li>
                        <li class="nav-item">
                            <a><%= link_to "Logout", destroy_user_session_path, class: "navbar-link", method: :delete %></a>
                        </li>
                        <li class="nav-item">
                            <% if current_user.avatar.attached? %>
                                <a><%= link_to(image_tag(current_user.avatar, class: "navbar-avatar"), "/users/#{current_user.id}") %></a>
                            <% end %>
                        </li>
                    </ul>

                <% else %>

                    <ul>
                        <li>
                            <a class="nav-link"><%= link_to "Two-Wheeled Buy & Sell", "/", class: "navbar-logo" %></a>
                        </li>
                    </ul>
                <% end %>
            </div>
        </div>

        <%= yield %>
        </div>

    </body>
</html>

